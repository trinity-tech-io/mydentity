import { RuntimeSettings } from "../model/settings";

export const defaultSettings: RuntimeSettings = {
  accessKey: null,
  appDID: null,
  webServiceEndpoint: "https://ownmydentity.com",
  webServiceAPIEndpoint: "https://api.ownmydentity.com"
}
